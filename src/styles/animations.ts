import { keyframes } from 'styled-components';

export const Up100 = keyframes`
  0% {bottom: -100%; }
  100% {bottom: 0; }
`;

export const Down100 = keyframes`
  0% {bottom: 0%; }
  100% {bottom: -100%; }
`;
