import styled from 'styled-components';

export const StyledButtonX = styled.div`
  font-family: ${({ theme }) => theme.fonts.style.bold};
  font-size: ${({ theme }) => theme.fonts.size.small};
`;
